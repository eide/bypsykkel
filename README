Prerequisites: node, yarn

How to run:
 - `yarn install`
 - `cp config.json.example config.json`
 - Edit config.json and add your client identifier
 - Run with `yarn start`
 - Go to http://localhost:3000/ in your browser

Testin with `yarn test`
