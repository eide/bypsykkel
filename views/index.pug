html
  head
    title Bypsykkel
    link(
      type="text/css"
      rel="stylesheet"
      href="/style.css"
    )
    script.
      window.stations = JSON.parse(`!{JSON.stringify(stations)}`);
    script(
      src='/mappy.js'
    )
    script(
      async
      src=`https://maps.googleapis.com/maps/api/js?key=${mapsApiKey}&callback=initMap`
    )

  body
    header
      h1 Bypsykkel

    main
      div(class='map')
      table
        thead
          tr
            th Stativ
            th Ledige sykler
            th Ledige låser

        tbody
          each station in stations
            tr
              td= station.title
              td= station.bikes
              td= station.locks
